{
  "name": "@kingdomos/cli",
  "version": "0.1.0",
  "private": true,
  "description": "KingdomOS CLI — init, configure, spawn agents, monitor tasks",
  "bin": {
    "kingdom": "./dist/bin/kingdom.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@kingdomos/core": "workspace:*",
    "@kingdomos/providers": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.4.5"
  }
}
